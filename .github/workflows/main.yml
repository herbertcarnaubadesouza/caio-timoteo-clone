name: Deploy Next.js Static

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v3

      - name: Instalar dependências
        run: npm install

      - name: Build + Export
        run: npm run build

      - name: Listar arquivos (opcional)
        run: ls -R out

      # 1) Deploy apenas dos arquivos de texto / HTML
      - name: Deploy Text Files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          keepalive: true
          timeout: 180000 # Increased timeout
          server: ftp.monomitofilmes.com.br
          username: monomitofilmes
          password: Huehuebr12
          protocol: ftp
          port: 21
          force: true
          concurrent-uploads: 1 # Reduced uploads to prevent errors
          passive: true # Use active mode
          auto-retry: true # Retry on failure
          disable-verification: true
          server-dir: "/home/monomitofilmes/www/"
          local-dir: "./out/contato.html/"
          exclude: |
            **/*.png
            **/*.jpg
            **/*.jpeg
            **/*.gif
            **/*.svg
            **/*.mp4
            **/*.mov
            **/.git*
            **/.github*/
            **/node_modules*/
            _next/
            _next/**
            404
            404.html

      # - name: Deploy Images
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          keepalive: true
          timeout: 180000
          server: ftp.monomitofilmes.com.br
          username: monomitofilmes
          password: Huehuebr12
          protocol: ftp
          port: 21
          force: true
          concurrent-uploads: 1
          passive: true
          auto-retry: true
          disable-verification: true
          server-dir: "/home/monomitofilmes/www/"
          local-dir: "./out/"
          exclude: |
            **/*.html
            **/*.txt
            **/*.js
            **/*.json
            **/*.css
            **/.git*
            **/.github*/
            **/node_modules*/
            _next/
            _next/**
            404
            404.html
